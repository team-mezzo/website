<h1> Donations </h1>

<%= render 'form' %>

<ul>
	<% for donation in @donations %>
		<li id = "donation_<%= donation.id %>">
			<%= donation.description %> 
			| <%= link_to 'Edit', edit_donation_path(donation)%> 
			| <%= link_to 'Delete', donation, :method => 'delete' %>
		</li>
	<% end %>
</ul>
